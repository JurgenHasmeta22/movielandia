model Genre {
    id   Int    @id @default(autoincrement())
    name String @default("Other")

    // Relations
    movies               MovieGenre[]
    series               SerieGenre[]
    usersWhoBookmarkedIt UserGenreFavorite[]
}
