import LoadingSpinner from "@/components/root/ui/loadingSpinner/LoadingSpinner";

const Loading = () => {
    return (
        <section>
            <LoadingSpinner />
        </section>
    );
};

export default Loading;
