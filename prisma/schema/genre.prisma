model Genre {
  id                   Int                 @id @default(autoincrement())
  name                 String              @default("")
  movies               MovieGenre[]
  series               SerieGenre[]
  usersWhoBookmarkedIt UserGenreFavorite[]
}
