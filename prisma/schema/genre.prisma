model Genre {
  id                   Int                 @id @default(autoincrement())
  name                 String              @default("")
  
  // Relations
  movies               MovieGenre[]
  series               SerieGenre[]
  
  usersWhoBookmarkedIt UserGenreFavorite[]
}
