import LoadingSpinner from "@/components/loadingSpinner/LoadingSpinner";

const Loading = () => {
    return (
        <section>
            <LoadingSpinner />
        </section>
    );
};

export default Loading;
